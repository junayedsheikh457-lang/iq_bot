from money_manager import MoneyManager\nfrom strategy import breakout_signal\n\nclass BotEngine:\n    def __init__(self, config):\n        self.config=config\n        self.manager=MoneyManager([float(x) for x in config['stakes']], int(config['max_consecutive_losses']))\n        self.last_signal=None\n\n    def on_candle_closes(self, closes):\n        signal=breakout_signal(closes, int(self.config['lookback']))\n        if signal is None:\n            return None\n        if not self.manager.active:\n            self.manager.new_signal_cycle()\n        self.last_signal=signal\n        return {'side':signal.side,'price':signal.price,'support':signal.support,'resistance':signal.resistance,'stake':self.manager.current_stake}\n\n    def on_result(self, won: bool):\n        if won:\n            self.manager.on_win()\n        else:\n            self.manager.on_loss()\n